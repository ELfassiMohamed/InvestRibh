[build]
  command = "npm run build"
  publish = "dist/client"

# SSR fallback — all unmatched routes go to the Netlify Function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions-internal/server"
  status = 200
