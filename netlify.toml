[build]
  command = "npm run build"
  publish = "dist/client"

# SPA fallback — all routes served by index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
